<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tags: <%= tagname %></title>
    <link rel="stylesheet" href="/css/index.css">
    <link rel="shortcut icon" href="/logo/favicon.png" type="image/x-icon">
    <script src="/css/tailwindcss.css"></script>
    <script src="/js/fontawesome.js" defer></script>
</head>

<body class="bg-slate-100 font-sans">

    <%- include('../../Component/Header') %>

    <main class="mx-auto px-4 xl:max-w-7xl mt-5 mb-5">
        <section class="flex-1 w-full h-full p-2 md:p-5 overflow-y-auto">
            
            <!-- Advertisement Section -->
            <div class="bg-white rounded-lg md:mb-12 mb-2 p-4">
                <div class="text-center mb-4">
                    <p class="text-sm text-gray-500" style="user-select: none;">Sponsored</p>
                </div>
                <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-6579807593228261"
                    crossorigin="anonymous"></script>
                <ins class="adsbygoogle block mx-auto" style="display:block; text-align:center; width: 50%;"
                    data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-6579807593228261"
                    data-ad-slot="5440757573"></ins>
                <script>
                    (adsbygoogle = window.adsbygoogle || []).push({});
                </script>
            </div>

            <!-- Articles Section -->
            <div class="bg-white px-6 md:px-12 py-8 md:py-12 mt-8 shadow-sm rounded-lg">
                <h1 class="text-2xl font-bold mb-6 text-gray-900">หมวดหมู่: <%= tagname %></h1>
                
                <% if (tags && tags.length > 0) { %>
                    <ul class="space-y-6">
                        <% tags.forEach(post => { %>
                            <li class="border-b pb-4 flex flex-col md:flex-row items-start md:space-x-4">
                                
                                <!-- Thumbnail Image -->
                                <div class="w-full md:w-1/4 mb-4 md:mb-0">
                                    <% if (post.thumbnail.startsWith('http')) { %>
                                        <img src="<%= post.thumbnail %>" alt="<%= post.title %>"
                                            class="w-full h-64 object-cover rounded-lg shadow-lg">
                                    <% } else { %>
                                        <img src="/uploads/thumbnails/<%= post.thumbnail %>" alt="<%= post.title %>"
                                            class="w-full h-64 object-cover rounded-lg shadow-lg">
                                    <% } %>
                                </div>

                                <!-- Article Content -->
                                <div class="w-full md:w-3/4">
                                    <h2 class="text-lg font-semibold text-gray-800">
                                        <a href="/read/<%= post.urlslug %>" class="hover:underline text-blue-500">
                                            <%= post.title %>
                                        </a>
                                    </h2>
                                </div>
                            </li>
                        <% }) %>
                    </ul>
                <% } else { %>
                    <p class="text-gray-700 mt-4">ไม่พบผลลัพธ์ที่ตรงกับ "<%= tagname %>"</p>
                <% } %>
            </div>
        </section>
    </main>

    <%- include('../../Component/Footer') %>

    <script src="/js/script.js" defer></script>
</body>

</html>
