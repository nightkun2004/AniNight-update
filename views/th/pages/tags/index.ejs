<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tags: <%= tagname %></title>
    <link rel="stylesheet" href="/css/index.css">
    <link rel="shortcut icon" href="/logo/favicon.png" type="image/x-icon">
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <script src="https://unpkg.com/boxicons@2.1.4/dist/boxicons.js"></script>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://kit.fontawesome.com/df26bd9708.js" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>
    <style>
         body::-webkit-scrollbar {
            display: none;
        }

        section {
            overflow-y: auto;
            overflow-x: hidden;
        }
    </style>
</head>
<body class="overflow-hidden">

    <main class="bg-white flex flex-row w-full h-screen">
        <%- include('../../Component/Sidebar') %>
        <section class="flex-1 w-full h-full p-2 md:p-5 overflow-y-scroll">
            <!-- Advertisement Section -->
            <div class="ads-container my-8 p-4 bg-gray-50 rounded-lg shadow-md border border-gray-200 aspect-w-16 aspect-h-9">
                <div class="text-center mb-4">
                    <p class="text-sm text-gray-500 " style="user-select: none;">Sponsored</p>
                </div>
                <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-6579807593228261"
                    crossorigin="anonymous"></script>
                <ins class="adsbygoogle block mx-auto" style="display:block; text-align:center; width: 50%;"
                    data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-6579807593228261"
                    data-ad-slot="5440757573"></ins>
                <script>
                    (adsbygoogle = window.adsbygoogle || []).push({});
                </script>
            </div>
    
            <div class="bg-white px-6 md:px-12 py-8 md:py-12 mt-8 shadow-sm h-full">
                <h1 class="text-2xl font-bold mb-6">หมวดหมู่: <%= tagname %></h1>
                <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
                    <% if (tags && tags.length > 0) { %>
                        <% tags.forEach(post => { %>
                            <div class="bg-white shadow-md rounded-lg overflow-hidden">
                                <a href="/read/<%= post.urlslug %>">
                                    <% if (post.thumbnail.startsWith('http')) { %>
                                        <img src="<%= post.thumbnail %>" alt="<%= post.title %>" class="w-full h-56 object-cover">
                                      <% } else { %>
                                        <img src="/uploads/thumbnails/<%= post.thumbnail %>" alt="<%= post.title %>" class="w-full h-56 object-cover">
                                      <% } %>        
                                </a>
                                <div class="p-4">
                                    <h2 class="text-lg font-semibold text-gray-900"><a href="/read/<%= post.urlslug %>" target="_blank" rel="noopener noreferrer"><%= post.title %></a></h2>
                                    <% if (post.creator && post.creator.id) { %>
                                        <div class="mt-3 flex items-center">
                                            <img class="h-8 w-8 rounded-full" src="/uploads/profiles/<%= post.creator.id.profilePicture %>" alt="Profile">
                                            <div class="ml-3">
                                                <p class="text-sm font-medium text-gray-900"><%= post.creator.id.username %></p>
                                                <p class="text-sm text-gray-500"><%= post.views %> views • <%= new Date(post.createdAt).toLocaleDateString() %></p>
                                            </div>
                                        </div>
                                    <% } %>
                                    
                                </div>
                            </div>
                        <% }); %>
                    <% } else { %>
                        <p class="text-gray-500">ไม่พบโพสต์ในหมวดหมู่นี้</p>
                    <% } %>
                </div>
            </div>
        </section>
    </main>

   

    <script src="/js/script.js"></script>
</body>
</html>