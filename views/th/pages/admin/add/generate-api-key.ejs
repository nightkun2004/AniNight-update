<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>สร้าง API KEY</title>
    <link rel="stylesheet" href="https://cdn.ani-night.online/aninight/css/index.css">
    <link rel="shortcut icon" href="/logo/favicon.png" type="image/x-icon">
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://kit.fontawesome.com/df26bd9708.js" crossorigin="anonymous"></script>
</head>
<body>
    <%- include('../../../Component/Header') %>
    
    <main class="w-full flex justify-center p-6 bg-white rounded-lg shadow-lg">
        <div>
            <h2 class="text-2xl font-bold text-center mb-6">อัปโหลด PV อนิเมะ</h2>
            <div class="mb-6 mt-4">
                <% if (typeof message !== 'undefined') { %>
                    <div class="mb-4 bg-gray-100 rounded-lg p-2 text-emerald-500 text-center"><%= message %></div>
                <% } %>
                <% if (typeof apiKey !== 'undefined') { %>
                    <div class="mb-4 bg-gray-100 rounded-lg p-2 text-emerald-500 text-center"><%= apiKey %></div>
                <% } %>
            </div>
            <form action="/admin/generate-api-key" method="POST">
                <label class="block text-sm font-medium text-gray-700" for="owner">Owner:</label>
                <input class="mt-1 block outline-none w-full border border-gray-300 rounded-md shadow-sm p-2 mb-4" type="text" id="owner" name="owner" required>
                <button class="w-full bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline" type="submit">Generate API Key</button>
            </form>
    </main>

    <script src="/js/script.js"></script>
    
</body>
</html>