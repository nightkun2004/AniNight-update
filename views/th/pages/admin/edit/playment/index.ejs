<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>แก้ไขรางวัล</title>
    <link rel="stylesheet" href="/css/index.css">
    <link rel="shortcut icon" href="/logo/favicon.png" type="image/x-icon">
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://kit.fontawesome.com/df26bd9708.js" crossorigin="anonymous"></script>
</head>
<body>
    <%- include('../../../../Component/Header') %>
    <%- include('../../../../Component/Sidebar') %>
    <main>
        <div class="bg-white shadow-md rounded-lg p-4">
            <div class="flex justify-between items-center mb-4">
                <h1 class="text-xl font-semibold text-gray-800">แก้ไขรางวัล</h1>
            </div>

            <form action="/admin/update/reward/<%= reward._id %>" method="POST">
                <div class="mb-4">
                    <label for="type" class="block text-gray-700 text-sm font-bold mb-2">ประเภทของรางวัล:</label>
                    <input type="text" id="type" name="type" value="<%= reward.playmentrequire.type %>" class="border-2 px-4 py-3 rounded-lg mt-1 block w-full" >
                </div>
                <div class="mb-4">
                    <label for="points" class="block text-gray-700 text-sm font-bold mb-2">คะแนน:</label>
                    <input type="number" id="points" name="points" value="<%= reward.playmentrequire.points %>" class="border-2 px-4 py-3 rounded-lg mt-1 block w-full" >
                </div>
                <div class="mb-4">
                    <label for="wallet" class="block text-gray-700 text-sm font-bold mb-2">แลกได้ (บาท):</label>
                    <input type="number" id="wallet" name="wallet" value="<%= reward.playmentrequire.wallet %>" class="border-2 px-4 py-3 rounded-lg mt-1 block w-full" >
                </div>
                <div class="mb-4">
                    <label for="published" class="block text-gray-700">เผยแพร่</label>
                    <input type="checkbox" id="published" name="published" <%= reward.published ? 'checked' : '' %> class="mt-2">
                </div>
                <div class="flex justify-end mt-6">
                    <button type="submit" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
                        <i class="fas fa-save"></i> บันทึกการเปลี่ยนแปลง
                    </button>
                </div>
            </form>
        </div>
    </main>

    <script src="/js/script.js"></script>
</body>
</html>
