<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>แก้ไขแบนเนอร์</title>
    <link rel="stylesheet" href="/css/index.css">
    <link rel="shortcut icon" href="/logo/favicon.png" type="image/x-icon">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/croppie/2.6.5/croppie.min.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/croppie/2.6.5/croppie.min.js"></script>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://kit.fontawesome.com/df26bd9708.js" crossorigin="anonymous"></script>
    <style>
        #upload-demo {
            width: 100%;
            height: 400px;
            position: relative;
        }
        .cr-boundary {
            width: 100% !important;
            height: 100% !important;
        }
        .cr-vp-square {
            width: 640px !important;
            height: 360px !important;
        }
    </style>
</head>
<body class="bg-blue-50">
    <%- include('../../../Component/Sidebar') %>
    <div class="flex-1 flex flex-col">
        <%- include('../../../Component/Header') %>
        <main class="mx-auto mt-10 mb-10 p-8 bg-white rounded-lg shadow-lg w-full max-w-2xl">
            <h1 class="text-2xl font-semibold text-gray-800 mb-9">
                <a href="/<%= userID.user.username %>">
                    <i class="fa-solid fa-arrow-left"></i> กลับ
                </a>
            </h1>
            <div class="mb-6 mt-4">
                <% if (typeof message !== 'undefined') { %>
                    <div class="mb-4 bg-gray-100 rounded-lg p-2 text-green-500 text-center"><%= message %></div>
                <% } %>
                <div id="message" class="message mb-4 bg-gray-100 rounded-lg p-2 text-green-500 text-center" style="display: none;"></div>
                <div id="errormessage" class="errormessage mb-4 bg-gray-100 rounded-lg p-2 text-red-500 text-center" style="display: none;"></div>
                <% if (typeof error !== 'undefined') { %>
                    <div class="mb-4 bg-gray-100 rounded-lg p-2 text-red-500 text-center"><%= error %></div>
                <% } %>
                <% if (typeof dataprofile !== 'undefined') { %>
                    <div class="mb-4 bg-gray-100 rounded-lg p-2 text-sky-500 text-center"><%= dataprofile %></div>
                <% } %>
            </div>
            <h2 class="text-center font-bold text-2xl">เลือกไฟล์ที่นี่เพื่ออัพโหลดภาพแบนเนอร์ของคุณ</h2>
            <p class="text-center">ขนาดภาพต้อง 2560 x 1440</p>
            <form id="upload-form" action="/api/v2/editprofile/editbanner/add" method="post" class="mt-5 mb-7" enctype="multipart/form-data">
                <div class="mt-5">
                    <label for="">เลือกไฟล์ที่นี่</label>
                    <input type="file" id="file-upload-modal" accept="image/*" class="block w-full text-sm text-gray-500 file:mr-4 file:py-2 file:px-4 file:rounded-full file:border-0 file:text-sm file:font-semibold file:bg-blue-50 file:text-blue-700 hover:file:bg-blue-100">
                    <div id="upload-demo" style="display: none;"></div>
                </div>
                <button type="submit" class="w-full px-6 mt-5 mb-5 py-3 bg-blue-500 text-white text-lg font-medium rounded-lg shadow hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500">บันทึก</button>
            </form>
        </main>
    </div>
    <script src="/js/script.js"></script>
    <script src="/js/auth/banner.js"></script>
</body>
</html>
