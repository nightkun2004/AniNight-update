<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ตารางอนิเมะ</title>
    <link rel="stylesheet" href="/css/index.css">
    <link rel="shortcut icon" href="/logo/favicon.png" type="image/x-icon">
    
    <meta property="og:image" content="https://m1r.ai/ycqx2.jpg">
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="630">
    <meta property="og:site_name" content="ani-night.online">
   
    
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://kit.fontawesome.com/df26bd9708.js" crossorigin="anonymous"></script>
   
</head>
<body class="flex flex-col min-h-screen bg-gray-100">
    <%- include('../../Component/Sidebar') %>
    <%- include('../../Component/Header') %>
        <main class="flex flex-col md:flex-row gap-4">
            <div class="container mx-auto p-4">
                <div class="mb-4 flex gap-4 items-center">
                    <h1 class="text-xl font-bold border-black border-b-2"><%= translations.welcome %></h1>
                </div>
                <div class="bg-white shadow rounded p-4">
                    <!-- ประเภทอนิเมะ -->
                    <div class="mb-4">
                        <h3 class="font-semibold mb-2"><%= translations.pages.schedule.animetype %></h3>
                        <div class="flex flex-wrap gap-2">
                            <button class="bg-blue-200 text-blue-600 rounded px-3 py-1 filter-btn" data-animetype="all"><%= translations.pages.schedule.all %></button>
                            <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-animetype="japan"><%= translations.pages.schedule.japanlang %></button>
                        </div>
                    </div>
        
                    <!-- categories -->
                    <div class="mb-4">
                        <h3 class="font-semibold mb-2"><%= translations.pages.schedule.stylesesstion %></h3>
                        <div class="flex flex-wrap gap-2">
                            <button class="bg-blue-200 text-blue-600 rounded px-3 py-1 filter-btn" data-style="ทั้งหมด"><%= translations.pages.schedule.all %></button>
                            <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-style="อนิเมะ"><%= translations.pages.schedule.japanlang %></button>
                            <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-style="มังงะ"><%= translations.pages.schedule.mangacat %></button>
                            <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-style="ชีวิตประจำวัน"><%= translations.pages.schedule.lifecat %></button>
                            <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-style="sci-fi"><%= translations.pages.schedule.scificat %></button>
                            <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-style="นิยาย"><%= translations.pages.schedule.lncat %></button>
                            <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-style="ผจญภัย"><%= translations.pages.schedule.adventurecat %></button>
                            <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-style="โมเอะ"><%= translations.pages.schedule.Moecat %></button>
                        </div>
                    </div>
        
                   <!-- Voice Actors -->
        <div class="mb-4">
            <h3 class="font-semibold mb-2"><%= translations.pages.schedule.voiceActors.title %></h3>
            <div class="flex flex-wrap gap-2">
                <button class="bg-blue-200 text-blue-600 rounded px-3 py-1 filter-btn" data-voice="all"><%= translations.pages.schedule.voiceActors.all %></button>
                <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-voice="พากย์ไทย"><%= translations.pages.schedule.voiceActors.thai %></button>
                <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-voice="พากย์ญี่ปุ่น"><%= translations.pages.schedule.voiceActors.japan %></button>
            </div>
        </div>

        <!-- Price -->
        <div class="mb-4">
            <h3 class="font-semibold mb-2"><%= translations.pages.schedule.price.title %></h3>
            <div class="flex flex-wrap gap-2">
                <button class="bg-blue-200 text-blue-600 rounded px-3 py-1 filter-btn" data-price="all"><%= translations.pages.schedule.price.all %></button>
                <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-price="free"><%= translations.pages.schedule.price.free %></button>
                <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-price="premium"><%= translations.pages.schedule.price.premium %></button>
            </div>
        </div>
        
                   <!-- season -->
                    <div class="mb-4">
                        <h3 class="font-semibold mb-2"><%= translations.pages.schedule.season.title %></h3>
                        <div class="flex flex-wrap gap-2">
                            <button class="bg-blue-200 text-blue-600 rounded px-3 py-1 filter-btn" data-season="all"><%= translations.pages.schedule.season.all %></button>
                            <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-season="Spring"><%= translations.pages.schedule.season.spring %></button>
                            <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-season="Summer"><%= translations.pages.schedule.season.summer %></button>
                            <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-season="Fall"><%= translations.pages.schedule.season.fall %></button>
                            <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-season="Winter"><%= translations.pages.schedule.season.winter %></button>
                        </div>
                    </div>

                    
                <!-- Year -->
                <div class="mb-4">
                    <h3 class="font-semibold mb-2"><%= translations.pages.schedule.year.title %></h3>
                    <div class="flex flex-wrap gap-2 year">
                        <button class="bg-blue-200 text-blue-600 rounded px-3 py-1 filter-btn" data-year="all"><%= translations.pages.schedule.year.all %></button>
                        <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-year="2025"><%= translations.pages.schedule.year.year2025 %></button>
                        <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-year="2024"><%= translations.pages.schedule.year.year2024 %></button>
                        <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-year="2023"><%= translations.pages.schedule.year.year2023 %></button>
                    </div>
                </div>
        
                      <!-- เดือน -->
                    <div class="mb-4">
                        <h3 class="font-semibold mb-2"><%= translations.pages.schedule.month.title %></h3>
                        <div class="flex flex-wrap gap-2 year">
                            <button class="bg-blue-200 text-blue-600 rounded px-3 py-1 filter-btn" data-month="all"><%= translations.pages.schedule.month.all %></button>
                            <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-month="มกราคม"><%= translations.pages.schedule.month.january %></button>
                            <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-month="กุมภาพันธ์"><%= translations.pages.schedule.month.february %></button>
                            <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-month="มีนาคม"><%= translations.pages.schedule.month.march %></button>
                            <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-month="เมษายน"><%= translations.pages.schedule.month.april %></button>
                            <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-month="พฤษภาคม"><%= translations.pages.schedule.month.may %></button>
                            <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-month="มิถุนายน"><%= translations.pages.schedule.month.june %></button>
                            <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-month="กรกฎาคม"><%= translations.pages.schedule.month.july %></button>
                            <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-month="สิงหาคม"><%= translations.pages.schedule.month.august %></button>
                            <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-month="กันยายน"><%= translations.pages.schedule.month.september %></button>
                            <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-month="ตุลาคม"><%= translations.pages.schedule.month.october %></button>
                            <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-month="พฤศจิกายน"><%= translations.pages.schedule.month.november %></button>
                            <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-month="ธันวาคม"><%= translations.pages.schedule.month.december %></button>
                        </div>
                    </div>
        
                    <!-- Status --> 
                    <div class="mb-4">
                        <h3 class="font-semibold mb-2"><%= translations.pages.schedule.status.title %></h3>
                        <div class="flex flex-wrap gap-2">
                            <button class="bg-blue-200 text-blue-600 rounded px-3 py-1 filter-btn" data-status="all"><%= translations.pages.schedule.status.all %></button>
                            <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-status="completed"><%= translations.pages.schedule.status.completed %></button>
                            <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn" data-status="incomplete"><%= translations.pages.schedule.status.incomplete %></button>
                        </div>
                    </div>
                    <!-- แพลตฟอร์ม -->
                    <div class="mb-4">
                        <h3 class="font-semibold mb-2"><%= translations.pages.schedule.platform.title %></h3>
                        <div class="flex flex-wrap gap-2">
                            <button class="bg-blue-200 text-blue-600 rounded px-3 py-1 filter-btn" data-platform="all"><%= translations.pages.schedule.platform.all %></button>
                            <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn"
                                data-platform="bilibili">BILIBILI</button>
                            <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn"
                                data-platform="crunchyroll">Crunchyroll</button>
                            <button class="bg-gray-200 text-gray-600 rounded px-3 py-1 filter-btn"
                                data-platform="iqiyi">iqiyi</button>
                        </div>
                    </div>

                    <div class="mb-4">
                        <h3 class="font-semibold mb-2"><%= translations.pages.schedule.search.title %></h3>
                        <input type="text" id="search-input" class="p-2 border rounded outline-none" placeholder="<%= translations.pages.schedule.search.placeholder %>">
                    </div>


                </div>
                <!-- Language Selector -->
                <div class="mt-4">
                    <button class="px-4 py-2 text-gray-500">เลือก: <span id="selected-filters" class="text-black">ทั้งหมด</span></button>
                </div>

                <div id="loading-content" class="loading-content flex justify-center" style="display: none;">
                    <div class="flex gap-2 items-center">
                        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" preserveAspectRatio="xMidYMid" width="50" height="50" style="shape-rendering: auto; display: block; background: transparent;" xmlns:xlink="http://www.w3.org/1999/xlink">
                            <g>
                                <circle stroke-dasharray="169.64600329384882 58.548667764616276" r="36" stroke-width="3" stroke="#5bbfe1" fill="none" cy="50" cx="50">
                                    <animateTransform keyTimes="0;1" values="0 50 50;360 50 50" dur="0.6211180124223602s" repeatCount="indefinite" type="rotate" attributeName="transform"></animateTransform>
                                </circle>
                            </g>
                        </svg>
                        <span>กำลังโหลดเนื้อหา.....</span>
                    </div>
                </div>
                
                
                <div class="grid grid-cols-2 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-1 md:gap-3 lg:gap-4" id="anime-container">
                    
                    <% if (Animelists && Animelists.length > 0) {%>
                    <% Animelists.forEach(anime => { %>
                     <% if (anime.published) {%>
                    <div class="anime-card bg-white shadow rounded overflow-hidden" id="main-content" style="display: none;"
                        data-animetype="<%= anime.animetype %>" data-categories="<%= anime.categories %>" data-voice="<%= anime.voice %>"
                        data-price="<%= anime.price %>" data-year="<%= anime.year %>"
                        data-status="<%= anime.status ? 'completed' : 'incomplete' %>" data-season="<%= anime.season %>"
                        data-month="<%= anime.month %>" 
                        <% if(anime.platforms && anime.platforms.length > 0) { %>
                            data-platforms="<%= JSON.stringify(anime.platforms) %>"
                        <% } %>>
                    
                        <picture class="relative">
                            <a href="/anime/<%= anime.urlslug %>">
                                <img width="400px" height="600px"
                            src="<%= anime.poster ? '/uploads/posters/' + anime.poster : 'https://via.placeholder.com/400x200' %>"
                            alt="<%= anime.title %>">
                            </a>
                            <span class="absolute top-0 left-0 bg-sky-400 rounded text-white px-3"><%= anime.year %></span>
                            <streeming class="absolute bottom-1 left-1 flex gap-2 select-none pointer-events-none items-center">
                                <% if (anime.platforms.includes('crunchyroll')) { %>
                                    <img src="/logo/crunchyroll_icon.png" alt="crunchyroll" class="w-7 h-7 pointer-events-none rounded-full">
                                <% } %>
                                <% if (anime.platforms.includes('bilibili')) { %>
                                    <img src="/logo/bilibili_icon.png" alt="bilibili" class="w-7 h-7 pointer-events-none rounded-full">
                                <% } %>
                                <% if (anime.platforms.includes('iqiyi')) { %>
                                    <img src="/logo/iqiyi_icon.png" alt="iqiyi" class="w-7 h-7 pointer-events-none rounded-full">
                                <% } %>
                                
                            </streeming>
                        </picture>
                        <div class="p-4">
                            <h3 class="text-lg font-semibold">
                                <a href="/anime/<%= anime.urlslug %>">
                                    <%= anime.title %>
                                </a>
                            </h3>
                            <p class="text-gray-600 text-sm">
                                <%= anime.voice %> • <%= anime.categories %>
                            </p>
                           
                        </div>

                    </div>
                     <% } %>
                    <% }) %>
                    <% } else { %>
                        <div class="bg-yellow-100 border-l-4 border-yellow-500 text-yellow-700 p-4 rounded-md shadow-md max-w-md mx-auto">
                            <div class="flex">
                                <div class="ml-3">
                                    <div class="flex-shrink-0 mb-6">
                                        <i class="fa-solid fa-database text-4xl"></i>
                                    </div>
                                    <h3 class="text-lg font-semibold">การย้ายข้อมูลกำลังดำเนินการอยู่</h3>
                                    <div class="mt-2 text-sm">
                                        <p>ขณะนี้เรากำลังย้ายข้อมูล กรุณารอสักครู่ ระบบจะพร้อมใช้งานในเร็ว ๆ นี้ ขออภัยในความไม่สะดวก</p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    <% } %>
                    <!-- Add more anime cards as needed -->
                </div>
               
            </div>
        </main>

<script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-6579807593228261"
    crossorigin="anonymous"></script>
<ins class="adsbygoogle" style="display:block" data-ad-format="autorelaxed" data-ad-client="ca-pub-6579807593228261"
    data-ad-slot="3649329378"></ins>
<script>
    (adsbygoogle = window.adsbygoogle || []).push({});
</script>
<script src="/js/script.js"></script>
<script src="/js/stanime.js"></script>
</body>

</html>